import React from 'react';
import SidePanel from '../sidePanel/sidePanel';

const Chat = () => {
    return (
        <div>
            <SidePanel/>
        </div>
    );
};

export default Chat;